import GameType from "../../../types/GameTypes";

const gameTypeStringToNumberMap = {
    assault: GameType.Assault,
    escort: GameType.Escort,
    hybrid: GameType.Hybrid,
    control: GameType.Control
};

export default gameTypeStringToNumberMap
