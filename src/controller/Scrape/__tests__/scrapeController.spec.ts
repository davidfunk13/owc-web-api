describe("Scraping API Tests", () => {
    describe("Get battletags by name", () => {
        //scratchpad, group theses tests accoringly...
        it.todo("should return a paginated list of battletags matching the name input given.")

        it.todo("Should check the cache for a key matching the name input, parse and return paginated dataset if found.")

        it.todo("It should make an api call to fetch a list of battletags with the given name input, then set key value pair containing battletag:JSON[listOfBattletags] in the cache.")
    })
})


